StringBuffer
    线程安全，可变字符序列
    JDK5开始被StringBuilder,但没有被同步
Vector
    线程安全，如果不需要建议ArrayList代替Vector
    该类改进了List接口，使其成为java Collection Framework的成员
     List<String> li=Collections.synchronizedList(new ArrayList<String>()); //包装成线程安全list
Hashtable

    线程安全，实现了一个哈希表。如果不需要建议HashMap代替Vector
    实现了Map接口，使其成为java Collection Framework的成员
    Map<String,String> m=Collections.synchronizedMap(new HashMap<>());